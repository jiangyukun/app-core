import path from 'path'

const project = 'D:/2018/web/edc/'
export const repoRoot = path.resolve(__dirname, '../')

export const srcRoot = path.join(repoRoot, 'src/')
export const libRoot = project + 'node_modules/app-core/'
